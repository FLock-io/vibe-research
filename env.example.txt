# Base URL for the application (used for API calls during SSR)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# FLock LLM API Configuration (OpenAI-compatible schema)
FLOCK_API_KEY=your_flock_api_key_here
FLOCK_API_BASE_URL=https://api.flock.io/v1
FLOCK_MODEL=flock-llm-default

# Vercel KV (for production data storage)
# These will be automatically set by Vercel when you add KV storage
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Cron Secret (for securing the refresh endpoint)
CRON_SECRET=your_random_secret_here

